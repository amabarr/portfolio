import * as React from "react";
import { Layout, Music, SEO } from "../components";

const music = () => (
	<>
		<Layout>
			<SEO title='Music' />
			<Music />
		</Layout>
	</>
);

export default music;
